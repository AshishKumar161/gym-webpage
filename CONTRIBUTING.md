# Contributing to A² ReVamp Gym

Thank you for contributing!

## Development Setup
1. Clone the repository.
2. Run `npm install` in the root workspace.
3. Start the dev server: `npm run dev`
4. Run tests: `npm test`
